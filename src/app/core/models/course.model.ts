export interface CourseData {
  id?: number;
  title: string;
  description: string;
  estimatedDuration: string; 
  level: number;
}


export interface CourseDTO {
  title: string;
  description: string;
  estimatedDuration: string;
  level: number;
}